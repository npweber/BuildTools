Buildtools
_____________________

I made this to automatically run MobWars plugin builds in development. It's basically just simple move file method, along with the running of spigot.jar wrapped into the utility. An old systems util that is shelved.